#include "main.h"

/**
 * _islower - checks whether a number is lowercase
 * @c:  checks if the input, c is lowercase or uppercase
 * Return:0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
