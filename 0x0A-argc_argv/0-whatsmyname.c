#include "main.h"

/**
 * main - prints the program name
 *@argc: the size of the command line argument
 *@argv: the pointer array of characters
 *Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
